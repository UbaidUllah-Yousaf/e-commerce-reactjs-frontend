/** Product listing PLP — filters, sort, pagination, grid (query params). */
export const SHOP_PRODUCTS_PATH = '/products'

/** Editorial collections directory */
export const SHOP_COLLECTIONS_PATH = '/collections'
